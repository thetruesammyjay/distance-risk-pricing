export default function MethodologyPage() { return <main className="mx-auto max-w-4xl px-6 py-16"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-teal">Methodology</p><h1 className="mt-3 text-4xl font-semibold tracking-tight text-ink">A transparent calculation pipeline.</h1><div className="mt-10 space-y-8 text-slate-600"><Section title="1. Route distance">The backend routing service obtains distance and duration from the configured routing provider. The browser does not calculate the authoritative trip distance.</Section><Section title="2. Route risk">Available component scores are normalized to the 0–1 range and combined with configured weights. Missing components are surfaced; they are not silently replaced with invented values.</Section><Section title="3. Demand">The prototype can use a deterministic simulated scenario. Its multiplier is capped and returned with a simulated source label.</Section><Section title="4. Pricing">The active formula and coefficient version are stored with the quote. The default additive strategy follows the research formula; an experimental multiplicative strategy is also supported.</Section><Section title="Limitations">Prototype coefficients are not calibrated transportation tariffs. Simulated, questionnaire-derived, and experimental inputs must not be presented as verified real-world safety or market data.</Section></div></main>; }

function Section({ title, children }: { title: string; children: React.ReactNode }) { return <section><h2 className="text-xl font-semibold text-ink">{title}</h2><p className="mt-2 leading-7">{children}</p></section>; }

