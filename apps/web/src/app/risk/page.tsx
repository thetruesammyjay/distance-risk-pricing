export default function RiskPage() { return <main className="mx-auto max-w-4xl px-6 py-16"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-teal">Risk view</p><h1 className="mt-3 text-4xl font-semibold tracking-tight text-ink">Risk information needs a label.</h1><p className="mt-5 max-w-2xl leading-8 text-slate-600">The prototype keeps accident, road, and security components separate. Questionnaire-derived values describe perceived security risk; they are not verified crime probabilities or guarantees of personal safety.</p><div className="mt-10 grid gap-4 md:grid-cols-3">{['Accident exposure', 'Road conditions', 'Perceived security'].map((item) => <section key={item} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-soft"><h2 className="font-medium text-ink">{item}</h2><p className="mt-3 text-sm leading-6 text-slate-500">Component availability and provenance are returned with each quote.</p></section>)}</div></main>; }

