# Research data

Data flows from `raw/` to `interim/` to `processed/`. Original raw data must
remain untouched. `external/` is reserved for legitimate third-party data.

No empirical or personally identifying data are included in this scaffold.
Document provenance, licensing, acquisition date, preprocessing, and known
limitations before adding a dataset.

